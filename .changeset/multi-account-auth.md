---
"@googleworkspace/cli": patch
---

Implement multi-account authentication support. Users can now use the global `--account <name>` flag to isolate credentials and token caches for different Google accounts.
